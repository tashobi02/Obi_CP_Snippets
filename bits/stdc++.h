#ifndef ALL_CPP_HEADERS_H
#define ALL_CPP_HEADERS_H

// User-listed headers
#include <algorithm>   // Algorithms on sequences (e.g., sort, find)
#include <cassert>     // Assert function for debugging
#include <cctype>      // Character classification functions
#include <cmath>       // Mathematical functions
#include <cstdlib>     // General purpose functions (e.g., memory management)
#include <cstring>     // C-style string functions
#include <ctime>       // Date and time functions
#include <exception>   // Standard exceptions
#include <fstream>     // File stream objects
#include <functional>  // Function objects
#include <iomanip>     // Input/output manipulators
#include <iostream>    // Input/output stream objects
#include <iterator>    // Iterator classes
#include <limits>      // Numeric limits
#include <list>        // Doubly linked list
#include <locale>      // Localization and internationalization
#include <map>         // Map container
#include <numeric>     // Numeric operations (e.g., accumulate)
#include <queue>       // Queue container
#include <random>      // Random number generators
#include <set>         // Set container
#include <sstream>     // String stream objects
#include <stack>       // Stack container
#include <stdexcept>   // Standard exception classes
#include <string>      // String class and functions
#include <typeinfo>    // Runtime type information
#include <utility>     // Utility components (e.g., std::pair)
#include <vector>      // Dynamic array


// Additional headers
#include <array>               // For fixed-size arrays
#include <bitset>              // For fixed-size sequences of bits
#include <chrono>              // For time-related utilities
#include <complex>             // For complex numbers
#include <condition_variable>  // For thread synchronization
#include <deque>               // For double-ended queues
#include <filesystem>          // For file system operations
#include <forward_list>        // For singly linked lists
#include <future>              // For asynchronous programming
#include <memory>              // For smart pointers
#include <mutex>               // For mutual exclusion locks
#include <regex>               // For regular expressions
#include <scoped_allocator>    // For nested memory allocation
#include <shared_mutex>        // For shared read/write locks
#include <thread>              // For multithreading support
#include <tuple>               // For working with tuples
#include <unordered_map>       // For hash-based associative containers
#include <unordered_set>       // For hash-based set containers
#include <valarray>            // For numerical arrays


// C Compatibility headers
#include <cfloat>   // For floating-point properties
#include <climits>  // For numeric limits like INT_MAX
#include <cstdarg>  // For variable arguments
#include <cstddef>  // For defining types like std::size_t
#include <cstdint>  // For fixed-width integer types
#include <cwchar>   // For wide-character functions
#include <cwctype>  // For wide-character classification


// C++20 and later
#include <bit>        // For bit manipulation
#include <concepts>   // For template constraints
#include <coroutine>  // For coroutines
#include <numbers>    // For mathematical constants
#include <ranges>     // For working with ranges
#include <span>       // For contiguous ranges of elements


#endif  // ALL_CPP_HEADERS_H
